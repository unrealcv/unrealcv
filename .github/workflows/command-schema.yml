name: Command Schema

on:
  pull_request:
    paths:
      - 'Source/UnrealCV/Private/Commands/**'
      - 'Source/UnrealCV/Private/Server/CommandDispatcher.cpp'
      - 'client/python/unrealcv/**'
      - 'docs/reference/commands.rst'
      - 'docs/reference/commands_generated.rst.txt'
      - 'docs/reference/command_schema.json'
      - 'workflow/command_coverage.py'
      - 'workflow/test_runner.py'
      - 'tools/command_schema/**'
      - '.github/workflows/command-schema.yml'

jobs:
  schema-sync-and-coverage:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Regenerate command schema
        run: python tools/command_schema/generate_schema.py --repo-root . --output docs/reference/command_schema.json

      - name: Verify schema is committed
        run: |
          git diff --exit-code -- docs/reference/command_schema.json docs/reference/commands_generated.rst.txt || (echo "Generated command docs are out of date. Regenerate and commit." && exit 1)

      - name: Validate documentation and workflow coverage
        run: python tools/command_schema/validate_coverage.py --repo-root . --schema docs/reference/command_schema.json --strict
